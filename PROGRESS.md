# Прогресс Freeword — аудит и план

Обновлено: 2026-07-05. Источник: ручной аудит src/, supabase/schema.sql, config.ts;
прогон scripts/test-groups.mjs (12/12 OK).

Статусы: ✅ готово · 🟡 частично · ⬜ осталось

---

## Аудит по шагам

### Шаг 1 — Каркас ✅
Роутер (`App.tsx`), провайдеры (`AuthContext`), страницы Register/SetupProfile/
ForgotPassword/ChatPage, Zustand-сторы (`chat-store.ts`, `message-store.ts`). Подтверждено чтением `src/App.tsx`.

### Шаг 2 — Авторизация ✅
Регистрация, выбор username при настройке профиля, сброс пароля по email
(`ForgotPassword.tsx`), защита маршрутов (`RequireAuth.tsx`). Подтверждено.

### Шаг 3 — Список чатов + личный чат ✅
`useChats.ts` — realtime-подписка на `postgres_changes`, поиск по username
(`searchUsers` в `lib/chats.ts`), `getOrCreateDirectChat`.

### Шаг 4 — Реальное время в чате ✅
Пагинация (`fetchMessages` с курсором), идемпотентная отправка (`client_id` +
уникальный индекс `messages_chat_client_idx` в schema.sql), realtime через
`useMessages.ts`, статусы delivered/read (таблица `message_status`,
`markMessageDelivered`/`markMessageRead`), «печатает…» (`useTyping.ts`).

### Шаг 5 — Медиа ✅
Изображение/файл/голосовое/видео-кружок. Запись через `useAudioRecorder.ts` /
`useVideoRecorder.ts` (MediaRecorder), плееры `AudioPlayer.tsx` / `VideoNotePlayer.tsx`,
лимиты в `config.ts` (`MAX_ATTACHMENT_SIZE_BYTES`, `MAX_VOICE_DURATION_SECONDS`,
`MAX_VIDEO_NOTE_DURATION_SECONDS`), загрузка в Supabase Storage (`lib/storage.ts`).

### Шаг 6 — Доделки в переписке ✅
- Ответить — ✅ (`reply_to_id`, цитата в `MessageBubble.tsx`, `ChatView.tsx:137`)
- Редактировать своё — ✅ (`editMessage`, только текстовые, только свои — `MessageInput.tsx`)
- Удалить — ✅ **оба варианта**. «У всех» — как раньше (`deleteMessage`, только свои).
  «У себя» — новое: таблица `message_hidden_for_user` (своя RLS-политика,
  `auth.uid() = user_id` на select/insert), `hideMessageForMe` в `lib/messages.ts`,
  `fetchMessages`/`searchMessagesInChat` теперь исключают скрытые для текущего
  пользователя сообщения. `ConfirmDeleteModal.tsx` предлагает «удалить у себя»
  всегда, «удалить у всех» — только для своих сообщений. Проверено e2e-тестом
  `scripts/test-delete-for-me.mjs` (8/8 OK, включая проверку, что RLS не даёт
  скрыть сообщение «от имени» другого пользователя и что чужой список скрытых
  сообщений не читается).
- Переслать — ✅ (`ForwardModal.tsx`, `forwarded_from_name` снапшотится при пересылке)
- Поиск по сообщениям в чате — ✅ (`ChatSearchBar.tsx`, full-text `search_vector`
  в schema.sql, generated tsvector column + gin-индекс)

### Шаг 7 — Группы ✅
Создание (`NewGroupModal.tsx`), роли owner/admin/member, системные сообщения,
инвайт-ссылки (создание/переиспользование/вступление через RPC
`get_invite_by_token`, см. `supabase/fix_invite_lookup.sql`), управление участниками
и выход (`GroupMembersPanel.tsx`). **e2e-тест `scripts/test-groups.mjs` прогнан только
что — все 12 проверок (создание, роли, удаление, RLS-видимость, инвайт, повторное
вступление, выход) прошли против реальной БД.**
Чего нет: редактирование названия/аватара группы после создания.

### Шаг 8 — Уведомления 🟡
- Mute чата — ✅ сделано. `setChatMuted` в `lib/chats.ts` (правит только свою
  строку `chat_members`, под защитой той же RLS-политики, что и раньше —
  «members can update their own membership row»), `ChatWithMeta.muted` теперь
  доходит до UI, `ChatListItem.tsx` — долгое нажатие/правый клик открывает
  меню «Отключить/включить уведомления», иконка колокольчика-с-перечёркиванием
  у заглушенных чатов. Проверено `scripts/test-mute.mjs` (5/5 OK, включая то,
  что один участник не может заглушить чужую строку через RLS).
- Web Notification API — ✅ сделано. `src/lib/notifications.ts`: запрос
  разрешения при заходе в приложение (`ChatPage.tsx`), показ уведомления при
  входящем сообщении в `useChats.ts` — но только если чат не заглушен (mute) и
  не является одновременно «открытым прямо сейчас и вкладка видима» (то есть
  уведомление придёт и если открыт другой чат, и если вкладка просто свёрнута/в
  фоне). Клик по уведомлению переключает на нужный чат.
  Service Worker / полноценный push (работает даже при закрытой вкладке) — не
  сделано, осознанно вне MVP-блокера.

### Шаг 9 — Профиль и настройки 🟡
- Страница `/settings` — ✅ сделана (`src/pages/Settings.tsx`, маршрут в `App.tsx`,
  открывается кликом по своему профилю внизу сайдбара). Смена отображаемого
  имени, username (с проверкой занятости, исключая себя самого), био, аватара
  (загрузка в Supabase Storage). `updateProfile` в `lib/auth.ts`.
- Редактирование группы — ✅ сделано заодно (было отмечено как недостающее в
  шаге 7): владелец/админ могут переименовать группу и сменить её аватар прямо
  в панели участников (`GroupMembersPanel.tsx`), `updateChatInfo` в `lib/chats.ts`.
  Проверено `scripts/test-settings.mjs` (5/5 OK, включая то, что обычный участник
  не может переименовать группу — RLS блокирует).
- Переключатель темы и просмотр активных сессий — по-прежнему не сделаны
  (тема и так тёмная по умолчанию, активные сессии — отдельная небольшая задача
  на потом, не блокер MVP).

### Шаг 10 — Деплой ✅
Код на GitHub (`github.com/maratttty/volna-messenger`), сайт развёрнут на
**GitHub Pages**: **https://maratttty.github.io/volna-messenger/**
(Netlify использовался раньше, теперь полностью переехали на GitHub Pages).
Автодеплой через GitHub Actions (`.github/workflows/deploy.yml`) при каждом
`git push` в `main`. Env-переменные (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`)
заданы как GitHub Actions Secrets в настройках репозитория.

Заодно нашёл и починил реальный баг по пути: «Забыли пароль» отправляла
письмо со ссылкой на `/reset-password`, а такой страницы/маршрута не было —
добавил `src/pages/ResetPassword.tsx`.

### Шаг 11 — Стабилизация 🟡
Тестовые скрипты есть и кое-что реально покрывают: `smoke-test.mjs`,
`test-groups.mjs`, `test-message-actions.mjs`, `test-message-types.mjs`,
`test-storage-upload.mjs`, `test-mute.mjs`, `test-delete-for-me.mjs`,
`test-settings.mjs`, `realtime-test.mjs`, `cleanup-smoke-test.mjs`.

- Устойчивость к обрыву сети — ✅ сделано. `src/lib/network.ts` —
  `onNetworkRecovery()` слушает событие браузера `online` и возврат вкладки
  из фона (`visibilitychange`). `useChats.ts` и `useMessages.ts` подписаны на
  это плюс на статус самого realtime-канала (`SUBSCRIBED` после обрыва, не
  первый при монтировании) — в обоих случаях дозагружают то, что могло
  прийти, пока соединения не было: `useChats` перезапрашивает список чатов,
  `useMessages` запрашивает свежую страницу сообщений активного чата,
  сливает её с уже загруженной историей (без дублей, с сортировкой по
  времени) и обновляет статусы прочтения. Это браузерное поведение
  (обрыв сети, сворачивание вкладки) — тестовым скриптом на базе данных не
  проверяется, нужна ручная проверка в DevTools (Network → Offline, затем
  обратно Online, или просто свернуть/развернуть вкладку).
- Нагрузочный тест группы 50+ участников — ✅ сделано. `scripts/test-load-group.mjs`:
  создаёт 51 пользователя через admin API, массово добавляет их в группу одним
  запросом (как делает реальный `createGroup`), проверяет список участников
  с join на профили, отправку сообщения, видимость чата и сообщения у
  рядового участника через RLS — и удаляет за собой все тестовые аккаунты в
  конце независимо от результата. Все 8 проверок прошли: вставка 51 участника
  — 437мс, выборка списка — 362мс (заметно быстрее критерия «<1 сек»).
- Итоговый прогон по критериям готовности — см. чек-лист ниже.

## Чек-лист готовности MVP

| Критерий | Статус |
|---|---|
| Регистрация + переписка 1-на-1 со статусами | ✅ реализовано и протестировано (`test-message-actions.mjs` и ручная проверка) |
| Группа 50+ без потерь/дублей | ✅ `test-load-group.mjs`, 51/51, без дублей |
| Картинка/файл/голос/видео-кружок | ✅ реализовано (`test-message-types.mjs`, `test-storage-upload.mjs`) |
| Непрочитанные / «прочитано» / «печатает…» | ✅ реализовано и протестировано |
| Веб-уведомления | ✅ реализовано, подтверждено пользователем вручную в браузере |
| Поиск чатов и сообщений | ✅ реализовано (`ChatList` поиск + `ChatSearchBar` full-text) |
| Переподключение при обрыве сети | ✅ реализовано (`network.ts` + дозагрузка), браузерное поведение — формальной автоматической проверки нет, можно проверить вручную в DevTools (Network → Offline → Online) |
| Приложение не падает | ✅ сборка и типы чистые, явных крашей не замечено |
| Доступен на публичном URL | ✅ https://maratttty.github.io/volna-messenger/ |

Все девять критериев MVP выполнены.

---

## Открытый вопрос — Stories (не в ТЗ)

В `supabase/schema.sql` уже есть таблицы `stories` / `story_views` и в `config.ts` —
`STORY_LIFETIME_MS`, хотя в планах сторис не упомянуты и в MVP не входят.
UI для них в коде нет (ни компонентов, ни хуков). Похоже,
обсуждалось в другом разговоре, который не попал в этот файл. Не блокирует
дальнейшую работу по MVP — оставляю как есть в БД, в план MVP не включаю. Скажете
позже, если нужно явно решить судьбу этих таблиц.

---

## План до конца MVP (по приоритету)

1. ✅ **Удаление «у себя»** (доделка шага 6) — сделано, см. шаг 6 выше.
2. ✅ **Mute чата** (часть шага 8) — сделано, см. шаг 8 выше.
3. ✅ **Уведомления** (шаг 8) — сделано (Web Notification API), см. шаг 8 выше.
   Service Worker push — сознательно не делаю, не блокер MVP.
4. ✅ **Настройки профиля** (шаг 9) — сделано, см. шаг 9 выше.
5. ✅ **Устойчивость realtime** (часть шага 11) — сделано, см. шаг 11 выше.
6. ✅ **Деплой на публичный URL** (шаг 10) — сделано, см. шаг 10 выше.
7. ✅ **Финальная стабилизация** (шаг 11) — сделано, см. шаг 11 и чек-лист выше.

**Все 7 пунктов плана выполнены — MVP готов.**

---

## После MVP — доп. функции (2026-06-30, второй заход)

Сверх ТЗ, по отдельному запросу:

1. ✅ **Кликабельная цитата reply** — клик по цитате в ленте прокручивает и
   подсвечивает оригинал (`MessageBubble.tsx`, `MessageList.tsx`, `ChatView.tsx`).
2. ✅ Статусы доставки/присутствия/«печатает» — перепроверены, без изменений,
   уже были полностью рабочими.
3. ✅ Mute чата — перепроверен, без изменений, уже был полностью рабочим.
4. ✅ Поиск по сообщениям в чате — перепроверен, без изменений, уже был
   полностью рабочим.
5. ✅ **Эмодзи-пикер + GIF через GIPHY** — `EmojiGifPicker.tsx` (своя панель
   эмодзи без библиотек, `lib/emoji-data.ts`), GIF-поиск через GIPHY API
   (`lib/giphy.ts`, ключ в `VITE_GIPHY_API_KEY`). Tenor изначально не подошёл —
   сервис закрывается, пересели на GIPHY. GIF отправляются как тип `image`
   с прямой ссылкой на CDN GIPHY, без загрузки в свой Storage.

**Нужно сделать вручную:** добавить `VITE_GIPHY_API_KEY` как GitHub Actions Secret
в настройках репозитория — иначе GIF не будут работать на продакшн-сайте.

6. ✅ **Реакции на сообщения** — таблица `message_reactions` (одна реакция на
   пользователя на сообщение, новый эмодзи заменяет старый), RLS по членству
   в чате. Быстрый выбор эмодзи в контекстном меню сообщения (долгое
   нажатие/правый клик), пилюли реакций под сообщением, realtime-синхронизация.
   Проверено `scripts/test-reactions.mjs` (7/7 OK).
7. ✅ **Закреплённые сообщения** — сначала одно (`chats.pinned_message_id`), затем переработано
   в **multi-pin**: таблица `pinned_messages(chat_id, message_id, pinned_by, pinned_at)`, RPC-функции
   `pin_message_multi`/`unpin_message_multi`, хук `usePinnedMessages`. Баннер под шапкой циклически
   листает все закреплённые (счётчик N/Total), открепляет по X. Realtime через postgres_changes.
   Закрепить может любой участник. SQL-миграция: `supabase/add_multi_pinned_messages.sql`.

Языки RU+EN из раздела 4 ТЗ не включены отдельным пунктом — это объёмная
кросс-резущая задача (i18n-обвязка + перевод всех строк), имеет смысл делать
последней, после того как функциональность стабилизируется.

---

## Этап A — Стабилизация веба (ТЕКУЩИЙ, 2026-07-03)

По дорожной карте (CLAUDE.md): после MVP-готовности переходим к полировке продукта
до уровня «работает чётко без лагов» на мобильном.

### Сделано в этапе A:

- ✅ **Realtime-слой — аудит и 4 фикса** (коммит `8497eb2`):
  - `fetchMessages` фильтрует `deleted = false` (удалённые не возвращались после обновления)
  - `appendMessage` дедуплицирует по `id` И `client_id` (двойные сообщения при отправке)
  - `useChats.ts` — локальное обновление чат-листа вместо `fetchChats` на каждый INSERT
  - SQL: `alter publication supabase_realtime add table message_reactions` (реакции не летели в realtime)
- ✅ **Контекстное меню сообщения** (коммит `d1598a1`):
  - Оверлей и меню — siblings в portal, не вложены (фикс iOS Safari `position:fixed` баг)
  - `window.visualViewport` для правильных размеров с открытой клавиатурой
  - Логика позиции: сначала проверяет снизу, если не влезает — сверху
  - RAF-ретрай если `offsetWidth = 0` на первом проходе
- ✅ **Фикс CI/CD** (коммит `37b883a`): убран устаревший `align` prop в `ChatListItem.tsx`

### Сделано в этапе A (дополнение, 2026-07-05):

- ✅ **Удаление сообщений** — «у себя» и «у всех» (своё); для чужих — только «у себя»; группа-владелец/админ может удалить любое. `ConfirmDeleteModal` с `canDeleteForEveryone`.
- ✅ **Статус прочтения в списке чатов** — одна галочка (не прочитано) / две слитные (прочитано) рядом с временем для своих последних сообщений. `lastMessageReadByOther` в `ChatWithMeta`, вычисляется в `chats.ts`.
- ✅ **Галочки в пузыре сообщения** — время + Check/CheckCheck (lucide) в правом нижнем углу своих сообщений.
- ✅ **Индикаторы активности** — «печатает…» / «записывает голосовое…» (пульс-микрофон) / «записывает видео…» (пульс-камера). `useTyping` расширен типами `ActivityType`, `notifyActivity`/`notifyActivityStop`. `MessageInput` вызывает при старте/конце записи. Явный стоп-сигнал без ожидания TTL.
- ✅ **Контекстное меню чата** — переработано: позиция в точке нажатия (не по прямоугольнику строки, больше не уходит вниз экрана). Диалог удаления контекстный: прямой чат → «у себя»/«у всех», группа-участник → «Выйти», группа-владелец → «Выйти»/«Удалить группу». SQL-миграция: `supabase/add_delete_chat_functions.sql`.

### Добавлено в этапе A (2026-07-06, фикс удаления чатов):

- ✅ **Удалить чат у себя (личный чат)** — soft-delete через `hidden_at`/`hidden_before_at` в `chat_members`. `fetchChats` фильтрует `.is('hidden_at', null)`. Гонка устранена: сначала UPDATE в базе, потом убираем с экрана. `chat_members UPDATE` подписка добавлена в `useChats` для синхронизации без перезагрузки.
- ✅ **Триггер `messages_clear_chat_hidden`** — сбрасывает `hidden_at` когда собеседник отправляет новое сообщение. Чат возвращается только с новыми сообщениями, старая история скрыта через `hidden_before_at`.
- ✅ **RLS SELECT-политика `chat_members`** обновлена: `auth.uid() = user_id OR is_chat_member(...)`. Это нужно чтобы Supabase Realtime доставлял DELETE-события после удаления строк.

### Открытые баги (аудит 2026-07-06):

- ❌ **#7 Создать новый личный чат** — `getOrCreateDirectChat` не фильтрует `hidden_at`, находит скрытый чат, возвращает его ID, но чат не в сторе → экран «нет чатов». Фикс: при нахождении скрытого чата — снимать `hidden_at` и возвращать.
- ❌ **#9 Удалить чат у всех** — RPC `delete_direct_chat_for_all` вероятно не задеплоена в базе (`add_delete_chat_functions.sql` не запускался). Ошибка проглатывается `catch { /* ignore */ }`, чат убирается локально, возвращается после перезагрузки.
- ⚠️ **#12 Удалить группу (владелец)** — зависит от `delete_group_chat` RPC из того же файла. Если не запускался — та же проблема.

### Открытые задачи этапа A (по приоритету):

1. ⬜ Клавиатура на мобильном — input перекрывается когда открывается клавиатура
2. ⬜ Упоминания @ в группах (dropdown + уведомление)
3. ⬜ Профиль собеседника (попап при тапе на имя/аватар)
4. ⬜ Расширенные настройки
